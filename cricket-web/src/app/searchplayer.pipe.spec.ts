import { SearchplayerPipe } from './searchplayer.pipe';

describe('SearchplayerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchplayerPipe();
    expect(pipe).toBeTruthy();
  });
});